program: system/main.py
name: centralized-rul-grid
method: grid
description: Grid search over FedCMAPSS RUL tasks, learning rates, and training epochs in centralized mode.

command:
  - ${env}
  - ${interpreter}
  - -u
  - system/main.py
  - --wandb-log
  - --algorithm
  - Centralized_RUL
  - --centralized
  - --dataset
  - FedCMAPSSWindow
  - --data_root
  - dataset/FedCMAPSS/
  - --model
  - LSTM_RUL
  - ${args}

parameters:
  task:
    values: ["A"]
  local_learning_rate:
    values: [0.0001, 0.0005, 0.001, 0.005, 0.01, 0.05]
  global_rounds:
    values: [10, 20, 30, 40, 50, 100, 200]
  split:
    values: [0]
  batch_size:
    values: [16, 32, 64]

